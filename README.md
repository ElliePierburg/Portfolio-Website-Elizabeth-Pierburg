# Elizabeth Pierburg Portfolio

Files added:

- `index.html` — main page with sections to edit (About, Projects, Contact)
- `styles.css` — simple responsive styles
- `script.js` — tiny script for year and demo contact handler

## Preview locally

1. Open `index.html` in your browser to preview.
2. Replace placeholder text (Your Name, tagline, project entries, email).
3. Replace placeholder images with screenshots of your projects.
4. The email link on the homepage opens the visitor's email client.

## Publish with GitHub Pages

1. Push this repository to GitHub.
2. Open the repository's **Settings → Pages**.
3. Under **Build and deployment**, choose **Deploy from a branch**, then select `main` and `/ (root)`.
4. Save and wait for GitHub to publish the site. The shareable URL will be `https://<your-github-username>.github.io/<repository-name>/`.

 Optional next steps
 
 - Add real links for each project (`Live` and `Code`).
 - Add a `favicon.ico` and real images in an `assets/` folder.
 - Deploy to GitHub Pages, Netlify, or Vercel.
 
 Project content added
 
 - `auto_parking_brake.html` — detailed project page for "Automatic Parking Brake" (POC). Replace placeholder images in `assets/` with real photos named `auto_park_1.jpg` and `auto_park_2.jpg`.
 - Updated `index.html` with a project card linking to the new page and a downloadable `ElizabethPierburgResume2026.pdf` (already in workspace). 
 
 How you can help me finish this project
 
1. Put your two project photos into `assets/` and name them `auto_park_1.jpg` and `auto_park_2.jpg`.
	- If you want, I can move the images you uploaded into `assets/` for you — confirm and I'll place them at `assets/auto_park_1.jpg` and `assets/auto_park_2.jpg`.
 2. If you want me to populate the "Engineering Challenges" or quantitative results, paste the text or allow me to extract details from your resume file.
 3. Tell me if you want the contact form to submit to an email or a service (I can add a simple Formspree/Netlify function or a Node endpoint).
